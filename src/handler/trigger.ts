/**
 * trigger.ts is an example backend app which can be invoked manually in the
 * AWS lambda console to call a slash_command, listen for an incoming webhook
 * and finally do something with the trigger_id
 */

export const handler = async() => {
  console.log("starting the handler")
}